# WordPress export content (bulk)

The canonical **WXR** for this migration lives one level up:

- `../parisgaelsgaa.WordPress.2026-04-14.xml`

This folder exists so `./export/wordpress/content/` is present for migration tooling checks. No per-post JSON files are required: the runner parses the WXR directly.
